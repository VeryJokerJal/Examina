<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ExamLab.Views.ModuleManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ExamLab.Views"
    xmlns:converters="using:ExamLab.Converters"
    xmlns:models="using:ExamLab.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:QuestionCountFormatConverter x:Key="QuestionCountFormatConverter"/>
        <converters:ScoreFormatConverter x:Key="ScoreFormatConverter"/>
        <converters:OrderFormatConverter x:Key="OrderFormatConverter"/>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{ThemeResource SystemControlBackgroundAccentBrush}" Padding="16,12">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <TextBlock Text="模块管理" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                <Button Content="添加模块" Command="{Binding AddModuleCommand}" Style="{ThemeResource AccentButtonStyle}"/>
                <Button Content="保存配置" Command="{Binding SaveConfigurationCommand}" Style="{ThemeResource AccentButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧模块列表 -->
            <Border Grid.Column="0" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 模块列表标题 -->
                    <StackPanel Grid.Row="0" Margin="16,16,16,8">
                        <TextBlock Text="试卷模块" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedExam.Name}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    </StackPanel>

                    <!-- 模块列表 -->
                    <ListView Grid.Row="1" 
                              ItemsSource="{Binding SelectedExam.Modules}" 
                              SelectedItem="{Binding SelectedModule, Mode=TwoWay}"
                              Margin="8,0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ListItemBorderStyle}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 模块名称和类型 -->
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Name}" FontWeight="SemiBold"/>
                                        <ToggleSwitch Grid.Row="0" Grid.Column="1" IsOn="{Binding IsEnabled, Mode=TwoWay}" ToolTipService.ToolTip="启用/禁用模块"/>

                                        <!-- 模块类型 -->
                                        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Type}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Margin="0,2,0,0"/>

                                        <!-- 模块描述 -->
                                        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding Description}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>

                                        <!-- 统计信息 -->
                                        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12" Margin="0,8,0,0">
                                            <TextBlock Text="{Binding Questions.Count, Converter={StaticResource QuestionCountFormatConverter}}" FontSize="11" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                            <TextBlock Text="{Binding Score, Converter={StaticResource ScoreFormatConverter}}" FontSize="11" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                            <TextBlock Text="{Binding Order, Converter={StaticResource OrderFormatConverter}}" FontSize="11" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- 模块操作按钮 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="16" HorizontalAlignment="Center">
                        <Button Content="上移" Command="{Binding MoveModuleUpCommand}" CommandParameter="{Binding SelectedModule}" MinWidth="60"/>
                        <Button Content="下移" Command="{Binding MoveModuleDownCommand}" CommandParameter="{Binding SelectedModule}" MinWidth="60"/>
                        <Button Content="删除" Command="{Binding DeleteModuleCommand}" CommandParameter="{Binding SelectedModule}" MinWidth="60"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 右侧模块详情和配置 -->
            <Grid Grid.Column="1">
                <!-- 模块详情编辑 -->
                <ScrollViewer Visibility="{Binding SelectedModule, Converter={StaticResource NullToVisibilityConverter}}" Padding="16">
                    <StackPanel Spacing="16">
                        <!-- 模块基本信息 -->
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel Spacing="12">
                                <TextBlock Text="模块基本信息" Style="{StaticResource SubtitleTextStyle}"/>
                                
                                <TextBox Header="模块名称" Text="{Binding SelectedModule.Name, Mode=TwoWay}" Style="{StaticResource FormTextBoxStyle}"/>
                                
                                <ComboBox Header="模块类型" SelectedItem="{Binding SelectedModule.Type, Mode=TwoWay}" Style="{StaticResource FormComboBoxStyle}">
                                    <models:ModuleType>Excel</models:ModuleType>
                                    <models:ModuleType>Word</models:ModuleType>
                                    <models:ModuleType>PowerPoint</models:ModuleType>
                                    <models:ModuleType>CSharp</models:ModuleType>
                                    <models:ModuleType>Windows</models:ModuleType>
                                </ComboBox>
                                
                                <TextBox Header="模块描述" Text="{Binding SelectedModule.Description, Mode=TwoWay}" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Style="{StaticResource FormTextBoxStyle}"/>
                                
                                <NumberBox Header="模块分值" Value="{Binding SelectedModule.Score, Mode=TwoWay}" Minimum="0" Style="{StaticResource FormNumberBoxStyle}"/>
                                
                                <NumberBox Header="显示顺序" Value="{Binding SelectedModule.Order, Mode=TwoWay}" Minimum="1" Style="{StaticResource FormNumberBoxStyle}"/>
                                
                                <ToggleSwitch Header="启用模块" IsOn="{Binding SelectedModule.IsEnabled, Mode=TwoWay}"/>
                            </StackPanel>
                        </Border>

                        <!-- 模块统计信息 -->
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel Spacing="12">
                                <TextBlock Text="模块统计" Style="{StaticResource SubtitleTextStyle}"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" Spacing="4">
                                        <TextBlock Text="题目数量" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                        <TextBlock Text="{Binding SelectedModule.Questions.Count}" FontSize="20" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Column="1" Spacing="4">
                                        <TextBlock Text="操作点数量" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                        <TextBlock Text="{Binding TotalOperationPoints}" FontSize="20" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Column="2" Spacing="4">
                                        <TextBlock Text="模块分值" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                        <TextBlock Text="{Binding SelectedModule.Score}" FontSize="20" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- 快速操作 -->
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel Spacing="12">
                                <TextBlock Text="快速操作" Style="{StaticResource SubtitleTextStyle}"/>
                                
                                <StackPanel Spacing="8">
                                    <Button Content="进入模块配置" Command="{Binding EnterModuleConfigCommand}" CommandParameter="{Binding SelectedModule}" HorizontalAlignment="Stretch"/>
                                    <Button Content="复制模块" Command="{Binding CopyModuleCommand}" CommandParameter="{Binding SelectedModule}" HorizontalAlignment="Stretch"/>
                                    <Button Content="导出模块配置" Command="{Binding ExportModuleCommand}" CommandParameter="{Binding SelectedModule}" HorizontalAlignment="Stretch"/>
                                    <Button Content="重置模块" Command="{Binding ResetModuleCommand}" CommandParameter="{Binding SelectedModule}" HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- 模块类型说明 -->
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel Spacing="12">
                                <TextBlock Text="模块类型说明" Style="{StaticResource SubtitleTextStyle}"/>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="• Windows: 文件和文件夹操作（9种操作类型）" Style="{StaticResource InfoTextStyle}"/>
                                    <TextBlock Text="• C#: 程序代码配置和输出验证" Style="{StaticResource InfoTextStyle}"/>
                                    <TextBlock Text="• PowerPoint: 幻灯片操作（39个知识点）" Style="{StaticResource InfoTextStyle}"/>
                                    <TextBlock Text="• Excel: 电子表格操作和数据处理" Style="{StaticResource InfoTextStyle}"/>
                                    <TextBlock Text="• Word: 文档编辑和格式化操作" Style="{StaticResource InfoTextStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- 无选中模块时的提示 -->
                <StackPanel Style="{StaticResource EmptyStateStyle}" Visibility="{Binding SelectedModule, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}">
                    <TextBlock Text="请选择一个模块" FontSize="18" HorizontalAlignment="Center"/>
                    <TextBlock Text="在左侧选择模块来查看和编辑模块配置" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" HorizontalAlignment="Center"/>
                    
                    <StackPanel Spacing="8" Margin="0,16,0,0">
                        <TextBlock Text="支持的模块类型" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Windows • C# • PowerPoint • Excel • Word" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
