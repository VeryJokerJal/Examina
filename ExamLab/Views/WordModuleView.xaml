<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="ExamLab.Views.WordModuleView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:converters="using:ExamLab.Converters" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="using:ExamLab.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--  模块标题  -->
        <Border Grid.Row="0" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefault}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="16">
                    <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="White" Text="{Binding CurrentContentViewModel.Title}" />
                    <Button Command="{Binding AddQuestionCommand}" Content="添加题目" Style="{ThemeResource AccentButtonStyle}" />
                </StackPanel>

                <!--<StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                    <Button Background="{ThemeResource SystemAccentColorLight2}" Command="{Binding GenerateWordDocumentCommand}" Content="生成Word文档" Foreground="White" Style="{ThemeResource DefaultButtonStyle}" />
                </StackPanel>-->
            </Grid>
        </Border>

        <ScrollViewer x:Name="LayoutRoot" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <!--  模块描述设置区域  -->
                <Border Grid.Row="0" Margin="10" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="8">
                    <StackPanel Spacing="16">
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="模块设置" />
                        <TextBlock FontSize="13" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="配置当前模块的基本信息" />

                        <StackPanel Spacing="12">
                            <TextBlock FontSize="14" FontWeight="Medium" Text="模块名称" />
                            <TextBox PlaceholderText="请输入模块名称" Text="{Binding SelectedModule.Name, Mode=TwoWay}" />
                        </StackPanel>

                        <StackPanel Spacing="12">
                            <TextBlock FontSize="14" FontWeight="Medium" Text="模块描述" />
                            <TextBox Height="100" AcceptsReturn="True" PlaceholderText="请输入模块描述" Text="{Binding SelectedModule.Description, Mode=TwoWay}" TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Spacing="12">
                            <Button Command="{Binding ResetModuleDescriptionCommand}" Content="重置" Style="{ThemeResource DefaultButtonStyle}" />
                            <Button Command="{Binding SaveModuleDescriptionCommand}" Content="保存" Style="{ThemeResource AccentButtonStyle}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!--  主内容区域  -->
                <Grid Grid.Row="1" Padding="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  左侧题目列表  -->
                    <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="8 0 0 8">
                        <Grid Padding="10" RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" FontSize="16" FontWeight="SemiBold" Text="题目列表" />

                            <ListView Grid.Row="1" ItemsSource="{Binding CurrentContentViewModel.Module.Questions}" SelectedItem="{Binding CurrentContentViewModel.SelectedQuestion, Mode=TwoWay}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="10">
                                            <Grid.ContextFlyout>
                                                <MenuFlyout>
                                                    <MenuFlyoutItem Click="DeleteQuestion_Click" Tag="{Binding}" Text="删除题目">
                                                        <MenuFlyoutItem.Icon>
                                                            <SymbolIcon Symbol="Delete" />
                                                        </MenuFlyoutItem.Icon>
                                                    </MenuFlyoutItem>
                                                </MenuFlyout>
                                            </Grid.ContextFlyout>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0" FontWeight="SemiBold" Text="{Binding Title}" />
                                            <TextBlock Grid.Row="1" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" MaxLines="2" Text="{Binding Content}" TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="0,1,1,1" CornerRadius="0 8 8 0" Visibility="{Binding CurrentContentViewModel.SelectedQuestion, Converter={StaticResource NullToVisibilityConverter}}">
                        <!--  右侧内容区域  -->
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!--  题目详情  -->
                            <StackPanel Grid.Row="0" Spacing="8">
                                <TextBlock FontSize="16" FontWeight="SemiBold" Text="题目详情" />
                                <TextBox Header="题目标题" Text="{Binding CurrentContentViewModel.SelectedQuestion.Title, Mode=TwoWay}" />
                                <TextBox Height="100" AcceptsReturn="True" Header="题目解析" Text="{Binding CurrentContentViewModel.SelectedQuestion.Content, Mode=TwoWay}" TextWrapping="Wrap" />

                                <!--  Word文档文件路径  -->
                                <StackPanel Spacing="8">
                                    <TextBlock FontSize="14" FontWeight="SemiBold" Text="Word文档文件路径" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0" Margin="0,0,8,0" PlaceholderText="选择或输入Word文档文件路径（可选）" Text="{Binding CurrentContentViewModel.SelectedQuestion.DocumentFilePath, Mode=TwoWay}" TextWrapping="NoWrap" />
                                        <Button Grid.Column="1" Margin="0,0,4,0" Command="{Binding CurrentContentViewModel.SelectDocumentFileCommand}" Content="浏览..." Style="{ThemeResource DefaultButtonStyle}" ToolTipService.ToolTip="选择Word文档文件" />
                                        <Button Grid.Column="2" Command="{Binding CurrentContentViewModel.ClearDocumentFilePathCommand}" Content="清除" Style="{ThemeResource DefaultButtonStyle}" ToolTipService.ToolTip="清除文件路径" />
                                    </Grid>
                                </StackPanel>

                                <StackPanel Spacing="4">
                                    <TextBlock FontWeight="SemiBold" Text="题目总分" />
                                    <TextBlock FontSize="16" Foreground="{ThemeResource SystemAccentColor}" Text="{Binding CurrentContentViewModel.SelectedQuestion.TotalScore}" />
                                </StackPanel>
                            </StackPanel>

                            <!--  知识点选择  -->
                            <ScrollViewer Grid.Row="1" Margin="0,16,0,0">
                                <StackPanel Spacing="16">
                                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="Word知识点" />

                                    <!--  段落操作类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="段落操作" IsExpanded="True">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphFont" Content="设置段落的字体" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphFontSize" Content="设置段落的字号" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphFontStyle" Content="设置段落的字形" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphCharacterSpacing" Content="设置段落字间距" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphTextColor" Content="设置段落文字的颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphAlignment" Content="设置段落对齐方式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphIndentation" Content="设置段落缩进" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphLineSpacing" Content="设置行间距" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphDropCap" Content="首字下沉" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphSpacing" Content="设置段落间距" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphBorderColor" Content="设置段落边框颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphBorderStyle" Content="设置段落边框线型" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphBorderWidth" Content="设置段落边框线宽" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetParagraphShading" Content="设置段落底纹" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  页面设置类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="页面设置">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPaperSize" Content="设置纸张大小" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPageMargins" Content="设置页边距" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetHeaderText" Content="设置页眉中的文字" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetHeaderFont" Content="设置页眉中文字的字体" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetHeaderFontSize" Content="设置页眉中文字的字号" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetHeaderAlignment" Content="设置页眉中文字的对齐方式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetFooterText" Content="设置页脚中的文字" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetFooterFont" Content="设置页脚中文字的字体" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetFooterFontSize" Content="设置页脚中文字的字号" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetFooterAlignment" Content="设置页脚中文字的对齐方式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPageNumber" Content="设置页码" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPageBackground" Content="设置页面背景" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPageBorderColor" Content="设置页面边框颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPageBorderStyle" Content="设置页面边框线型" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPageBorderWidth" Content="设置页面边框线宽" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  水印设置类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="水印设置">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetWatermarkText" Content="设置水印文字" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetWatermarkFont" Content="设置水印文字的字体" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetWatermarkFontSize" Content="设置水印文字的字号" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetWatermarkOrientation" Content="设置水印文字水平或倾斜方式" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  项目符号与编号类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="项目符号与编号">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetBulletNumbering" Content="设置项目编号" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  表格操作类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="表格操作">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableRowsColumns" Content="设置表格的行数和列数" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableShading" Content="设置表格底纹" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableRowHeight" Content="设置表格行高" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableColumnWidth" Content="设置表格列宽" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableCellContent" Content="设置单元格内容" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableCellAlignment" Content="设置表格单元格对齐方式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableAlignment" Content="设置整个表格对齐方式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="MergeTableCells" Content="合并单元格" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableHeaderContent" Content="设置表头第一个单元格的内容" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTableHeaderAlignment" Content="设置表头第一个单元格的对齐方式" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  图形和图片设置类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="图形和图片设置">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="InsertAutoShape" Content="插入自选图形类型" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetAutoShapeSize" Content="设置自选图形大小" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetAutoShapeLineColor" Content="设置自选图形线条颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetAutoShapeFillColor" Content="设置自选图形填充颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetAutoShapeTextSize" Content="设置自选图形中文字大小" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetAutoShapeTextColor" Content="设置自选图形中文字颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetAutoShapeTextContent" Content="设置自选图形中文字内容" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetAutoShapePosition" Content="设置自选图形的位置" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetImageBorderCompoundType" Content="设置插入图片边框复合类型" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetImageBorderDashType" Content="设置插入图片边框短划线类型" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetImageBorderWidth" Content="设置插入图片边框线宽" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetImageBorderColor" Content="设置插入图片边框颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetImageShadow" Content="设置插入图片阴影类型与颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetImageWrapStyle" Content="设置插入图片环绕方式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetImageSize" Content="设置插入图片的高度和宽度" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetImagePosition" Content="设置插入图片的位置" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  文本框设置类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="文本框设置">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTextBoxBorderColor" Content="设置文本框边框颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTextBoxContent" Content="设置文本框中文字内容" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTextBoxTextSize" Content="设置文本框中文字大小" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTextBoxPosition" Content="设置文本框位置" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetTextBoxWrapStyle" Content="设置文本框环绕方式" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  其他操作类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="其他操作">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="FindAndReplace" Content="查找与替换" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetSpecificTextFontSize" Content="设置指定文字字号" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  操作说明  -->
                                    <InfoBar Title="操作说明" IsClosable="False" IsOpen="True" Message="选择Word知识点来添加操作配置。&#x0a;支持67个Word知识点，包括段落操作、页面设置、水印设置、表格操作、图形和图片设置、文本框设置、其他操作等。" Severity="Informational" />
                                </StackPanel>
                            </ScrollViewer>

                            <!--  操作点列表标题  -->
                            <TextBlock Grid.Row="2" Margin="0,16,0,8" FontSize="16" FontWeight="SemiBold" Text="操作点配置" />

                            <!--  操作点列表  -->
                            <Grid Grid.Row="3">
                                <ListView ItemsSource="{Binding CurrentContentViewModel.SelectedQuestion.OperationPoints}" SelectedItem="{Binding CurrentContentViewModel.SelectedOperationPoint, Mode=TwoWay}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Padding="10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                                    <TextBlock FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="{Binding Description}" />
                                                </StackPanel>
                                                <Button Grid.Column="1" Margin="0,0,8,0" Command="{Binding DataContext.CurrentContentViewModel.EditOperationPointCommand, ElementName=LayoutRoot}" CommandParameter="{Binding}" Content="编辑" ToolTipService.ToolTip="编辑操作点" />
                                                <Button Grid.Column="2" Command="{Binding DataContext.CurrentContentViewModel.DeleteOperationPointCommand, ElementName=LayoutRoot}" CommandParameter="{Binding}" Content="删除" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <!--  空操作点列表提示  -->
                                <StackPanel Padding="10" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="5" Spacing="12" Visibility="{Binding CurrentContentViewModel.SelectedQuestion.OperationPoints.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Zero}">
                                    <TextBlock HorizontalAlignment="Center" FontSize="16" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="暂无操作点" />
                                    <TextBlock HorizontalAlignment="Center" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="请在上方选择操作类型来添加操作点" />
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>

                    <!--  无选中题目时的提示  -->
                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="0,1,1,1" CornerRadius="0 8 8 0" Visibility="{Binding CurrentContentViewModel.SelectedQuestion, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                            <TextBlock HorizontalAlignment="Center" FontSize="18" Text="请选择一个题目" />
                            <TextBlock HorizontalAlignment="Center" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="在左侧选择题目来配置Word操作点" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
