<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="ExamLab.Views.ExcelModuleView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:converters="using:ExamLab.Converters" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="using:ExamLab.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--  模块标题  -->
        <Border Grid.Row="0" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefault}">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="White" Text="{Binding CurrentContentViewModel.Title}" />
                <Button Command="{Binding AddQuestionCommand}" Content="添加题目" Style="{ThemeResource AccentButtonStyle}" />
            </StackPanel>
        </Border>

        <ScrollViewer x:Name="LayoutRoot" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <!--  模块描述设置区域  -->
                <Border Grid.Row="0" Margin="10" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="8">
                    <StackPanel Spacing="16">
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="模块设置" />
                        <TextBlock FontSize="13" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="配置当前模块的基本信息" />

                        <StackPanel Spacing="12">
                            <TextBlock FontSize="14" FontWeight="Medium" Text="模块名称" />
                            <TextBox PlaceholderText="请输入模块名称" Text="{Binding SelectedModule.Name, Mode=TwoWay}" />
                        </StackPanel>

                        <StackPanel Spacing="12">
                            <TextBlock FontSize="14" FontWeight="Medium" Text="模块描述" />
                            <TextBox Height="100" AcceptsReturn="True" PlaceholderText="请输入模块描述" Text="{Binding SelectedModule.Description, Mode=TwoWay}" TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Spacing="12">
                            <Button Command="{Binding ResetModuleDescriptionCommand}" Content="重置" Style="{ThemeResource DefaultButtonStyle}" />
                            <Button Command="{Binding SaveModuleDescriptionCommand}" Content="保存" Style="{ThemeResource AccentButtonStyle}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!--  主内容区域  -->
                <Grid Grid.Row="1" Padding="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  左侧题目列表  -->
                    <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="8 0 0 8">
                        <Grid Padding="10" RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" FontSize="16" FontWeight="SemiBold" Text="题目列表" />

                            <ListView Grid.Row="1" ItemsSource="{Binding CurrentContentViewModel.Module.Questions}" SelectedItem="{Binding CurrentContentViewModel.SelectedQuestion, Mode=TwoWay}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="10">
                                            <Grid.ContextFlyout>
                                                <MenuFlyout>
                                                    <MenuFlyoutItem Click="DeleteQuestion_Click" Tag="{Binding}" Text="删除题目">
                                                        <MenuFlyoutItem.Icon>
                                                            <SymbolIcon Symbol="Delete" />
                                                        </MenuFlyoutItem.Icon>
                                                    </MenuFlyoutItem>
                                                </MenuFlyout>
                                            </Grid.ContextFlyout>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0" FontWeight="SemiBold" Text="{Binding Title}" />
                                            <TextBlock Grid.Row="1" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" MaxLines="2" Text="{Binding Content}" TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="0,1,1,1" CornerRadius="0 8 8 0" Visibility="{Binding CurrentContentViewModel.SelectedQuestion, Converter={StaticResource NullToVisibilityConverter}}">
                        <!--  右侧内容区域  -->
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!--  题目详情  -->
                            <StackPanel Grid.Row="0" Spacing="8">
                                <TextBlock FontSize="16" FontWeight="SemiBold" Text="题目详情" />
                                <TextBox Header="题目标题" Text="{Binding CurrentContentViewModel.SelectedQuestion.Title, Mode=TwoWay}" />
                                <TextBox Height="100" AcceptsReturn="True" Header="题目内容" Text="{Binding CurrentContentViewModel.SelectedQuestion.Content, Mode=TwoWay}" TextWrapping="Wrap" />

                                <!--  Excel文档文件路径  -->
                                <StackPanel Spacing="8">
                                    <TextBlock FontSize="14" FontWeight="SemiBold" Text="Excel文档文件路径" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0" Margin="0,0,8,0" PlaceholderText="选择或输入Excel文档文件路径（可选）" Text="{Binding CurrentContentViewModel.SelectedQuestion.DocumentFilePath, Mode=TwoWay}" TextWrapping="NoWrap" />
                                        <Button Grid.Column="1" Margin="0,0,4,0" Command="{Binding CurrentContentViewModel.SelectDocumentFileCommand}" Content="浏览..." Style="{ThemeResource DefaultButtonStyle}" ToolTipService.ToolTip="选择Excel文档文件" />
                                        <Button Grid.Column="2" Command="{Binding CurrentContentViewModel.ClearDocumentFilePathCommand}" Content="清除" Style="{ThemeResource DefaultButtonStyle}" ToolTipService.ToolTip="清除文件路径" />
                                    </Grid>
                                </StackPanel>

                                <StackPanel Spacing="4">
                                    <TextBlock Text="题目总分" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding CurrentContentViewModel.SelectedQuestion.TotalScore}" FontSize="16" Foreground="{ThemeResource SystemAccentColor}" />
                                </StackPanel>
                            </StackPanel>

                            <!--  知识点选择  -->
                            <ScrollViewer Grid.Row="1" Margin="0,16,0,0">
                                <StackPanel Spacing="16">
                                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="Excel知识点" />

                                    <!--  Excel基础操作类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="Excel基础操作" IsExpanded="True">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="FillOrCopyCellContent" Content="填充或复制单元格内容" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="MergeCells" Content="合并单元格" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetCellFont" Content="设置指定单元格字体" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetInnerBorderStyle" Content="内边框样式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetInnerBorderColor" Content="内边框颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetHorizontalAlignment" Content="设置单元格区域水平对齐方式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetNumberFormat" Content="设置目标区域单元格数字分类格式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="UseFunction" Content="使用函数" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetRowHeight" Content="设置行高" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetColumnWidth" Content="设置列宽" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetCellFillColor" Content="设置单元格填充颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetOuterBorderStyle" Content="设置外边框样式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetOuterBorderColor" Content="设置外边框颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetVerticalAlignment" Content="设置垂直对齐方式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ModifySheetName" Content="修改sheet表名称" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="AddUnderline" Content="添加下划线" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetFontStyle" Content="设置字型" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetFontSize" Content="设置字号" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetFontColor" Content="字体颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPatternFillStyle" Content="设置图案填充样式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetPatternFillColor" Content="设置填充图案颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ConditionalFormat" Content="条件格式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="SetCellStyleData" Content="设置单元格样式——数据" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  数据清单操作类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="数据清单操作">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="Filter" Content="筛选" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="Sort" Content="排序" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="Subtotal" Content="分类汇总" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="AdvancedFilterCondition" Content="高级筛选-条件" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="AdvancedFilterData" Content="高级筛选-数据" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="PivotTable" Content="数据透视表" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  图表操作类  -->
                                    <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="图表操作">
                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ChartType" Content="图表类型" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ChartStyle" Content="图表样式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ChartMove" Content="图表移动" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="CategoryAxisDataRange" Content="分类轴数据区域" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ValueAxisDataRange" Content="数值轴数据区域" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ChartTitle" Content="图表标题" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ChartTitleFormat" Content="图表标题格式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="HorizontalAxisTitle" Content="主要横坐标轴标题" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="HorizontalAxisTitleFormat" Content="主要横坐标轴标题格式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="LegendPosition" Content="设置图例位置" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="LegendFormat" Content="设置图例格式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="VerticalAxisOptions" Content="设置主要纵坐标轴选项" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="MajorHorizontalGridlines" Content="设置网格线——主要横网格线" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="MinorHorizontalGridlines" Content="设置网格线——次要横网格线" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="MajorVerticalGridlines" Content="主要纵网格线" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="MinorVerticalGridlines" Content="次要纵网格线" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="DataSeriesFormat" Content="设置数据系列格式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="AddDataLabels" Content="添加数据标签" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="DataLabelsFormat" Content="设置数据标签格式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ChartAreaFormat" Content="设置图表区域格式" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ChartFloorColor" Content="显示图表基底颜色" />
                                            <Button HorizontalAlignment="Stretch" Command="{Binding CurrentContentViewModel.AddOperationPointByTypeCommand}" CommandParameter="ChartBorder" Content="设置图表边框线" />
                                        </StackPanel>
                                    </Expander>

                                    <!--  操作说明  -->
                                    <InfoBar Title="操作说明" IsClosable="False" IsOpen="True" Message="选择Excel知识点来添加操作配置。&#x0a;支持51个Excel知识点，包括基础操作、数据清单操作、图表操作等。" Severity="Informational" />
                                </StackPanel>
                            </ScrollViewer>

                            <!--  操作点列表标题  -->
                            <TextBlock Grid.Row="2" Margin="0,16,0,8" FontSize="16" FontWeight="SemiBold" Text="操作点配置" />

                            <Grid Grid.Row="3">
                                <!--  操作点列表  -->
                                <ListView ItemsSource="{Binding CurrentContentViewModel.SelectedQuestion.OperationPoints}" SelectedItem="{Binding CurrentContentViewModel.SelectedOperationPoint, Mode=TwoWay}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Padding="10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                                    <TextBlock FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="{Binding Description}" />
                                                </StackPanel>
                                                <Button Grid.Column="1" Margin="0,0,8,0" Command="{Binding DataContext.CurrentContentViewModel.EditOperationPointCommand, ElementName=LayoutRoot}" CommandParameter="{Binding}" Content="编辑" ToolTipService.ToolTip="编辑操作点" />
                                                <Button Grid.Column="2" Command="{Binding DataContext.CurrentContentViewModel.DeleteOperationPointCommand, ElementName=LayoutRoot}" CommandParameter="{Binding}" Content="删除" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <!--  空操作点列表提示  -->
                                <StackPanel Padding="10" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="5" Spacing="12" Visibility="{Binding CurrentContentViewModel.SelectedQuestion.OperationPoints.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Zero}">
                                    <TextBlock HorizontalAlignment="Center" FontSize="16" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="暂无操作点" />
                                    <TextBlock HorizontalAlignment="Center" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="请在上方选择操作类型来添加操作点" />
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>

                    <!--  无选中题目时的提示  -->
                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="0,1,1,1" CornerRadius="0 8 8 0" Visibility="{Binding CurrentContentViewModel.SelectedQuestion, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                            <TextBlock HorizontalAlignment="Center" FontSize="18" Text="请选择一个题目" />
                            <TextBlock HorizontalAlignment="Center" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="在左侧选择题目来配置Excel操作点" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
