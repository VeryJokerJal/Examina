<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="ExamLab.Views.CSharpModuleView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:converters="using:ExamLab.Converters" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="using:ExamLab.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <converters:CSharpQuestionTypeConverter x:Key="CSharpQuestionTypeConverter" />
        <converters:CSharpQuestionTypeToVisibilityConverter x:Key="CSharpQuestionTypeToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--  模块标题  -->
        <Border Grid.Row="0" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefault}">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="White" Text="{Binding Title}" />
                <Button Command="{Binding AddQuestionCommand}" Content="添加题目" Style="{ThemeResource AccentButtonStyle}" />
            </StackPanel>
        </Border>

        <ScrollViewer x:Name="LayoutRoot" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <!--  模块描述设置区域  -->
                <Border Grid.Row="0" Margin="10" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="8">
                    <StackPanel Spacing="16">
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="模块设置" />
                        <TextBlock FontSize="13" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="配置当前模块的基本信息" />

                        <StackPanel Spacing="12">
                            <TextBlock FontSize="14" FontWeight="Medium" Text="模块名称" />
                            <TextBox PlaceholderText="请输入模块名称" Text="{Binding SelectedModule.Name, Mode=TwoWay}" />
                        </StackPanel>

                        <StackPanel Spacing="12">
                            <TextBlock FontSize="14" FontWeight="Medium" Text="模块描述" />
                            <TextBox Height="100" AcceptsReturn="True" PlaceholderText="请输入模块描述" Text="{Binding SelectedModule.Description, Mode=TwoWay}" TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Spacing="12">
                            <Button Command="{Binding ResetModuleDescriptionCommand}" Content="重置" Style="{ThemeResource DefaultButtonStyle}" />
                            <Button Command="{Binding SaveModuleDescriptionCommand}" Content="保存" Style="{ThemeResource AccentButtonStyle}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!--  主内容区域  -->
                <Grid Grid.Row="1" Padding="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  左侧题目列表  -->
                    <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="8 0 0 8">
                        <Grid Padding="10" RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" FontSize="16" FontWeight="SemiBold" Text="题目列表" />

                            <ListView Grid.Row="1" ItemsSource="{Binding Module.Questions}" SelectedItem="{Binding SelectedQuestion, Mode=TwoWay}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="10">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0" FontWeight="SemiBold" Text="{Binding Title}" />
                                            <TextBlock Grid.Row="1" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" MaxLines="2" Text="{Binding Content}" TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="0,1,1,1" CornerRadius="0 8 8 0" Visibility="{Binding SelectedQuestion, Converter={StaticResource NullToVisibilityConverter}}">
                        <!--  右侧内容区域  -->
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!--  题目详情  -->
                            <StackPanel Grid.Row="0" Spacing="8">
                                <TextBlock FontSize="16" FontWeight="SemiBold" Text="题目详情" />
                                <TextBox Header="题目标题" Text="{Binding SelectedQuestion.Title, Mode=TwoWay}" />
                                <TextBox Height="100" AcceptsReturn="True" Header="题目内容" Text="{Binding SelectedQuestion.Content, Mode=TwoWay}" TextWrapping="Wrap" />

                                <!--  C#题目类型选择  -->
                                <StackPanel Spacing="4">
                                    <ComboBox Header="题目类型" SelectedValue="{Binding SelectedQuestion.CSharpQuestionType, Mode=TwoWay, Converter={StaticResource CSharpQuestionTypeConverter}}" SelectedValuePath="Content">
                                        <ComboBoxItem Content="代码补全" />
                                        <ComboBoxItem Content="调试纠错" />
                                        <ComboBoxItem Content="编写实现" />
                                    </ComboBox>
                                </StackPanel>

                                <!--  分数设置区域  -->
                                <StackPanel Spacing="8">
                                    <TextBlock FontWeight="SemiBold" Text="分数设置" />

                                    <!--  调试纠错和编写实现类型的直接分数设置  -->
                                    <StackPanel Spacing="4" Visibility="{Binding SelectedQuestion.CSharpQuestionType, Converter={StaticResource CSharpQuestionTypeToVisibilityConverter}, ConverterParameter=Debugging}">
                                        <TextBlock FontSize="14" Text="题目分数" />
                                        <NumberBox Header="分数" Minimum="0" Maximum="1000" SpinButtonPlacementMode="Inline" Value="{Binding SelectedQuestion.CSharpDirectScore, Mode=TwoWay}" />
                                    </StackPanel>

                                    <StackPanel Spacing="4" Visibility="{Binding SelectedQuestion.CSharpQuestionType, Converter={StaticResource CSharpQuestionTypeToVisibilityConverter}, ConverterParameter=Implementation}">
                                        <TextBlock FontSize="14" Text="题目分数" />
                                        <NumberBox Header="分数" Minimum="0" Maximum="1000" SpinButtonPlacementMode="Inline" Value="{Binding SelectedQuestion.CSharpDirectScore, Mode=TwoWay}" />
                                    </StackPanel>

                                    <!--  题目总分显示  -->
                                    <StackPanel Spacing="4">
                                        <TextBlock FontWeight="SemiBold" Text="题目总分" />
                                        <TextBlock FontSize="16" Foreground="{ThemeResource SystemAccentColor}" Text="{Binding SelectedQuestion.TotalScore}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>

                            <!--  C#程序配置  -->
                            <ScrollViewer Grid.Row="1" Margin="0,16,0,0">
                                <StackPanel Spacing="16">
                                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="C#程序配置" />

                                    <!--  配置说明  -->
                                    <InfoBar Title="配置说明" IsClosable="False" IsOpen="True" Message="C#程序配置用于设置程序的输入参数和预期输出结果。&#x0a;• 程序参数：程序运行时的命令行参数&#x0a;• 控制台输出：程序预期的输出结果" Severity="Informational" />

                                    <!--  程序参数输入  -->
                                    <StackPanel Spacing="8">
                                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="程序参数输入" />
                                        <TextBox PlaceholderText="输入程序运行时的参数（可选）" Text="{Binding SelectedQuestion.ProgramInput, Mode=TwoWay}" TextWrapping="Wrap" />
                                    </StackPanel>

                                    <!--  程序控制台输出  -->
                                    <StackPanel Spacing="8">
                                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="程序控制台输出" />
                                        <TextBox Height="120" AcceptsReturn="True" PlaceholderText="输入程序预期的控制台输出结果" Text="{Binding SelectedQuestion.ExpectedOutput, Mode=TwoWay}" TextWrapping="Wrap" />
                                    </StackPanel>

                                    <!--  C#代码文件路径  -->
                                    <StackPanel Spacing="8">
                                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="C#代码文件路径" />
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox Grid.Column="0" Margin="0,0,8,0" PlaceholderText="选择或输入C#代码文件路径（可选）" Text="{Binding SelectedQuestion.CodeFilePath, Mode=TwoWay}" TextWrapping="NoWrap" />
                                            <Button Grid.Column="1" Margin="0,0,4,0" Command="{Binding SelectCodeFileCommand}" Content="浏览..." Style="{ThemeResource DefaultButtonStyle}" ToolTipService.ToolTip="选择C#代码文件" />
                                            <Button Grid.Column="2" Command="{Binding ClearCodeFilePathCommand}" Content="清除" Style="{ThemeResource DefaultButtonStyle}" ToolTipService.ToolTip="清除文件路径" />
                                        </Grid>
                                        <TextBlock FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="提示：可以选择一个C#代码文件作为题目的参考代码或模板文件" TextWrapping="Wrap" />
                                    </StackPanel>

                                    <!--  代码补全填空处（仅代码补全类型显示）  -->
                                    <StackPanel Spacing="12" Visibility="{Binding SelectedQuestion.CSharpQuestionType, Converter={StaticResource CSharpQuestionTypeToVisibilityConverter}, ConverterParameter=CodeCompletion}">
                                        <StackPanel Orientation="Horizontal" Spacing="12">
                                            <TextBlock VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Text="代码填空列表" />
                                            <Button Command="{Binding AddCodeBlankCommand}" Content="添加代码填空" Style="{ThemeResource AccentButtonStyle}" />
                                        </StackPanel>

                                        <TextBlock FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="提示：为代码补全题目添加多个代码填空，每个填空输入需要学生填写的代码内容" TextWrapping="Wrap" />

                                        <!--  填空处列表  -->
                                        <ScrollViewer>
                                            <ItemsControl ItemsSource="{Binding SelectedQuestion.CodeBlanks}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Margin="0,0,0,8" Padding="12" Background="{ThemeResource CardBackgroundFillColorSecondary}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="6">
                                                            <StackPanel Spacing="8">
                                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                                    <TextBlock VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold">
                                                                        <Run Text="代码填空 " />
                                                                        <Run Text="{Binding Order}" />
                                                                    </TextBlock>
                                                                    <Button Padding="8,4" Command="{Binding DataContext.DeleteCodeBlankCommand, ElementName=LayoutRoot}" CommandParameter="{Binding}" Content="删除" FontSize="12" Style="{ThemeResource DefaultButtonStyle}" />
                                                                </StackPanel>

                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*" />
                                                                        <ColumnDefinition Width="120" />
                                                                    </Grid.ColumnDefinitions>

                                                                    <TextBox Grid.Column="0" Margin="0,0,8,0" MaxHeight="500" AcceptsReturn="True" Header="代码内容" PlaceholderText="输入需要学生填写的代码内容（支持多行）" ScrollViewer.VerticalScrollBarVisibility="Auto" Text="{Binding Description, Mode=TwoWay}" TextWrapping="Wrap" />

                                                                    <NumberBox Grid.Column="1" Header="分数" Minimum="0" Maximum="100" SpinButtonPlacementMode="Inline" Value="{Binding Score, Mode=TwoWay}" />
                                                                </Grid>
                                                            </StackPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>

                                        <!--  无填空处时的提示  -->
                                        <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="6" Visibility="{Binding SelectedQuestion.CodeBlanks.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Zero}">
                                            <StackPanel HorizontalAlignment="Center" Spacing="8">
                                                <TextBlock HorizontalAlignment="Center" FontSize="14" Text="暂无代码填空" />
                                                <TextBlock HorizontalAlignment="Center" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="点击&quot;添加代码填空&quot;按钮开始创建" />
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!--  无选中题目时的提示  -->
                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="0,1,1,1" CornerRadius="0 8 8 0" Visibility="{Binding SelectedQuestion, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                            <TextBlock HorizontalAlignment="Center" FontSize="18" Text="请选择一个题目" />
                            <TextBlock HorizontalAlignment="Center" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="在左侧选择题目来配置C#程序" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
