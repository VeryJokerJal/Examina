&lt;ContentDialog x:Class="ExamLab.Views.ModuleTypeSelectionDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:vm="using:ExamLab.ViewModels"
               Title="选择模块类型"
               PrimaryButtonText="确定"
               SecondaryButtonText="取消"
               DefaultButton="Primary"
               mc:Ignorable="d">

    <ContentDialog.Resources>
        <Style x:Key="ModuleTypeItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="0,4" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefault}" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefault}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </ContentDialog.Resources>

    <Grid Width="500" Height="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 标题和说明 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16" Spacing="8">
            <TextBlock Text="选择专项试卷的模块类型" 
                       FontSize="16" 
                       FontWeight="SemiBold" />
            <TextBlock Text="专项试卷只包含一个模块，请选择您要创建的模块类型。" 
                       FontSize="14" 
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                       TextWrapping="Wrap" />
        </StackPanel>

        <!-- 模块类型列表 -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding ModuleTypes}"
                  SelectedItem="{Binding SelectedModuleType, Mode=TwoWay}"
                  SelectionMode="Single"
                  ItemContainerStyle="{StaticResource ModuleTypeItemStyle}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- 图标 -->
                        <Border Grid.Column="0" 
                                Width="48" 
                                Height="48" 
                                Background="{ThemeResource AccentFillColorDefaultBrush}" 
                                CornerRadius="24" 
                                Margin="0,0,16,0">
                            <SymbolIcon Symbol="{Binding Icon}" 
                                        Foreground="White" 
                                        FontSize="20" />
                        </Border>

                        <!-- 内容 -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                            <TextBlock Text="{Binding Name}" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Description}" 
                                       FontSize="14" 
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
