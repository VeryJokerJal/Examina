<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ExamLab.Views.QuestionManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ExamLab.Views"
    xmlns:converters="using:ExamLab.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <converters:ScoreFormatConverter x:Key="ScoreFormatConverter"/>
        <converters:OrderFormatConverter x:Key="OrderFormatConverter"/>
        <converters:OperationPointCountFormatConverter x:Key="OperationPointCountFormatConverter"/>
        <converters:ScoreDisplayConverter x:Key="ScoreDisplayConverter"/>
        <converters:ParameterCountFormatConverter x:Key="ParameterCountFormatConverter"/>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{ThemeResource SystemControlBackgroundAccentBrush}" Padding="16,12">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <TextBlock Text="题目管理" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                <TextBlock Text="{Binding SelectedModule.Name}" FontSize="14" Foreground="White" VerticalAlignment="Center"/>
                <Button Content="添加题目" Command="{Binding AddQuestionCommand}" Style="{ThemeResource AccentButtonStyle}"/>
                <Button Content="批量导入" Command="{Binding BatchImportCommand}" Style="{ThemeResource AccentButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧题目列表 -->
            <Border Grid.Column="0" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 搜索和筛选 -->
                    <StackPanel Grid.Row="0" Margin="16,16,16,8" Spacing="8">
                        <TextBox PlaceholderText="搜索题目..." Text="{Binding SearchText, Mode=TwoWay}"/>

                    </StackPanel>

                    <!-- 题目统计 -->
                    <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="16,0,16,8">
                        <StackPanel Spacing="4">
                            <TextBlock Text="题目统计" FontSize="12" FontWeight="SemiBold"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding TotalQuestions}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="总数" FontSize="10"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding DescriptionQuestions}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="描述" FontSize="10"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="{Binding ScoringQuestions}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="评分" FontSize="10"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 题目列表 -->
                    <ListView Grid.Row="2" 
                              ItemsSource="{Binding FilteredQuestions}" 
                              SelectedItem="{Binding SelectedQuestion, Mode=TwoWay}"
                              Margin="8,0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ListItemBorderStyle}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 题目标题和启用状态 -->
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Title}" FontWeight="SemiBold"/>
                                        <ToggleSwitch Grid.Row="0" Grid.Column="1" IsOn="{Binding IsEnabled, Mode=TwoWay}" Scale="0.8"/>

                                        <!-- 题目类型 -->
                                        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="{ThemeResource SystemControlBackgroundAccentBrush}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding Type}" FontSize="10" Foreground="White"/>
                                        </Border>

                                        <!-- 题目内容预览 -->
                                        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding Content}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap" MaxLines="2" Margin="0,4,0,0"/>

                                        <!-- 统计信息 -->
                                        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12" Margin="0,8,0,0">
                                            <TextBlock Text="{Binding Score, Converter={StaticResource ScoreFormatConverter}}" FontSize="11" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                            <TextBlock Text="{Binding Order, Converter={StaticResource OrderFormatConverter}}" FontSize="11" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                            <TextBlock Text="{Binding OperationPoints.Count, Converter={StaticResource OperationPointCountFormatConverter}}" FontSize="11" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- 题目操作按钮 -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8" Margin="16" HorizontalAlignment="Center">
                        <Button Content="上移" Command="{Binding MoveQuestionUpCommand}" CommandParameter="{Binding SelectedQuestion}" MinWidth="60"/>
                        <Button Content="下移" Command="{Binding MoveQuestionDownCommand}" CommandParameter="{Binding SelectedQuestion}" MinWidth="60"/>
                        <Button Content="复制" Command="{Binding CopyQuestionCommand}" CommandParameter="{Binding SelectedQuestion}" MinWidth="60"/>
                        <Button Content="删除" Command="{Binding DeleteQuestionCommand}" CommandParameter="{Binding SelectedQuestion}" MinWidth="60"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 右侧题目详情编辑 -->
            <ScrollViewer Grid.Column="1" Visibility="{Binding SelectedQuestion, Converter={StaticResource NullToVisibilityConverter}}" Padding="16">
                <StackPanel Spacing="16">
                    <!-- 题目基本信息 -->
                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="题目基本信息" Style="{StaticResource SubtitleTextStyle}"/>
                            
                            <TextBox Header="题目标题" Text="{Binding SelectedQuestion.Title, Mode=TwoWay}" Style="{StaticResource FormTextBoxStyle}"/>
                            
                            <TextBox Header="题目内容" Text="{Binding SelectedQuestion.Content, Mode=TwoWay}" TextWrapping="Wrap" AcceptsReturn="True" Height="120" Style="{StaticResource FormTextBoxStyle}"/>
                            
                            <StackPanel Spacing="4">
                                <TextBlock Text="题目总分" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding SelectedQuestion.TotalScore}" FontSize="16" Foreground="{ThemeResource SystemAccentColor}" />
                            </StackPanel>
                            
                            <NumberBox Header="显示顺序" Value="{Binding SelectedQuestion.Order, Mode=TwoWay}" Minimum="1" Style="{StaticResource FormNumberBoxStyle}"/>
                            
                            <ToggleSwitch Header="启用题目" IsOn="{Binding SelectedQuestion.IsEnabled, Mode=TwoWay}"/>
                        </StackPanel>
                    </Border>

                    <!-- 操作点管理 -->
                    <Border Style="{StaticResource CardBorderStyle}" Visibility="{Binding IsSelectedQuestionScoringType, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="16">
                                <TextBlock Text="操作点管理" Style="{StaticResource SubtitleTextStyle}"/>
                                <Button Content="添加操作点" Command="{Binding AddOperationPointCommand}"/>
                            </StackPanel>
                            
                            <TextBlock Text="评分题目需要配置操作点来定义具体的评分标准" Style="{StaticResource InfoTextStyle}"/>
                            
                            <!-- 操作点列表 -->
                            <ItemsControl ItemsSource="{Binding SelectedQuestion.OperationPoints}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource ParameterPanelStyle}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Name}" FontWeight="SemiBold"/>
                                                <Button Grid.Row="0" Grid.Column="1" Content="删除" Command="{Binding DataContext.DeleteOperationPointCommand, ElementName=LayoutRoot}" CommandParameter="{Binding}"/>
                                                
                                                <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Description}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Margin="0,4,0,0"/>
                                                
                                                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12" Margin="0,8,0,0">
                                                    <TextBlock Text="{Binding Score, Converter={StaticResource ScoreFormatConverter}}" FontSize="11"/>
                                                    <TextBlock Text="{Binding Parameters.Count, Converter={StaticResource ParameterCountFormatConverter}}" FontSize="11"/>
                                                    <Button Content="配置" Command="{Binding DataContext.ConfigureOperationPointCommand, ElementName=LayoutRoot}" CommandParameter="{Binding}" FontSize="11"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <!-- 无操作点提示 -->
                            <TextBlock Text="暂无操作点，请添加操作点来定义评分标准" 
                                       FontSize="14" 
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                       HorizontalAlignment="Center"
                                       Margin="0,16,0,0"
                                       Visibility="{Binding SelectedQuestion.OperationPoints.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Zero}"/>
                        </StackPanel>
                    </Border>

                    <!-- 题目预览 -->
                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="题目预览" Style="{StaticResource SubtitleTextStyle}"/>
                            
                            <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4" Padding="12">
                                <StackPanel Spacing="8">
                                    <TextBlock Text="{Binding SelectedQuestion.Title}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding SelectedQuestion.Content}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding SelectedQuestion.TotalScore, Converter={StaticResource ScoreDisplayConverter}}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- 快速操作 -->
                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="快速操作" Style="{StaticResource SubtitleTextStyle}"/>
                            
                            <StackPanel Spacing="8">
                                <Button Content="保存题目" Command="{Binding SaveQuestionCommand}" HorizontalAlignment="Stretch"/>
                                <Button Content="重置题目" Command="{Binding ResetQuestionCommand}" HorizontalAlignment="Stretch"/>
                                <Button Content="导出题目" Command="{Binding ExportQuestionCommand}" HorizontalAlignment="Stretch"/>
                                <Button Content="进入模块配置" Command="{Binding EnterModuleConfigCommand}" HorizontalAlignment="Stretch"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- 无选中题目时的提示 -->
            <StackPanel Grid.Column="1" Style="{StaticResource EmptyStateStyle}" Visibility="{Binding SelectedQuestion, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}">
                <TextBlock Text="请选择一个题目" FontSize="18" HorizontalAlignment="Center"/>
                <TextBlock Text="在左侧选择题目来查看和编辑题目详情" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" HorizontalAlignment="Center"/>
                
                <StackPanel Spacing="8" Margin="0,16,0,0">
                    <TextBlock Text="题目类型说明" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="• Description: 题目描述，仅用于说明" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="• ScoringDescription: 评分题目，需要配置操作点" FontSize="12" HorizontalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
